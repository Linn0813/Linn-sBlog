# 命令对比说明

## 📋 命令功能对比

### 1. `hexo s` / `hexo server` / `npm run server`

**功能**：本地预览当前配置的主题

**当前配置**：`theme: fluid`

**执行结果**：
- ✅ 启动本地服务器（默认 http://localhost:4000）
- ✅ 预览 **Fluid 主题**
- ❌ 不会切换主题
- ❌ 不会部署

**使用场景**：
- 本地开发调试
- 预览当前主题效果

**示例**：
```bash
hexo s
# 或
npm run server
```

---

### 2. `hexo d` / `hexo deploy` / `npm run deploy`

**功能**：部署当前配置的主题

**当前配置**：`theme: fluid`

**执行结果**：
- ✅ 生成静态文件（使用当前主题：Fluid）
- ✅ 部署到 GitHub Pages（根据 `_config.yml` 的 `deploy` 配置）
- ❌ 不会切换主题
- ❌ 只部署一个主题

**使用场景**：
- 部署当前主题到生产环境
- 后台服务自动部署

**示例**：
```bash
hexo d
# 或
npm run deploy
```

---

### 3. `npm run deploy:both`

**功能**：同时部署两个主题

**执行结果**：
- ✅ 先切换到 Butterfly，生成并部署到主仓库
- ✅ 再切换到 Fluid，生成并部署到 `fluid-blog` 仓库
- ⚠️ 最后会把主题切换回 Fluid（脚本最后切换了主题）
- ✅ 两个主题都会部署

**使用场景**：
- 需要同时更新两个主题版本
- 后台服务部署双主题

**示例**：
```bash
npm run deploy:both
```

---

## 🔄 预览不同主题

### 如果想预览 Butterfly 主题

```bash
# 方法 1：切换主题后预览
npm run theme:butterfly
hexo s

# 方法 2：一键切换并预览（可以添加快捷命令）
npm run switch:butterfly
hexo s
```

### 如果想预览 Fluid 主题

```bash
# 方法 1：当前已经是 Fluid，直接预览
hexo s

# 方法 2：确保是 Fluid 后预览
npm run theme:fluid
hexo s
```

---

## 💡 建议添加的快捷命令

可以在 `package.json` 中添加：

```json
{
  "scripts": {
    "preview:butterfly": "node tools/theme-switcher.js butterfly && hexo clean && hexo generate && hexo server",
    "preview:fluid": "node tools/theme-switcher.js fluid && hexo clean && hexo generate && hexo server"
  }
}
```

然后可以：
- `npm run preview:butterfly` - 预览 Butterfly 主题
- `npm run preview:fluid` - 预览 Fluid 主题

---

## 📊 完整对比表

| 命令 | 功能 | 切换主题 | 生成文件 | 部署 | 预览 |
|------|------|---------|---------|------|------|
| `hexo s` | 本地预览 | ❌ | ✅ | ❌ | ✅ |
| `hexo d` | 部署 | ❌ | ✅ | ✅ | ❌ |
| `npm run deploy:both` | 部署双主题 | ✅ | ✅ | ✅ | ❌ |
| `npm run theme:butterfly` | 切换主题 | ✅ | ❌ | ❌ | ❌ |
| `npm run switch:butterfly` | 切换+生成 | ✅ | ✅ | ❌ | ❌ |

---

## 🎯 总结

- **`hexo s`**：只预览当前配置的主题（现在是 Fluid）
- **`hexo d`**：只部署当前配置的主题（现在是 Fluid）
- **`npm run deploy:both`**：部署两个主题（会切换主题）

**如果只想预览/部署当前主题，直接用 `hexo s` 或 `hexo d` 就可以了！**
