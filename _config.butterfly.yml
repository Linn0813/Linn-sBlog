# ============================================
# Hexo 博客配置文件 - Butterfly 主题
# ============================================
# 文档: https://hexo.io/docs/configuration.html
# 源码: https://github.com/hexojs/hexo/

# ============================================
# 站点基本信息配置
# ============================================
# Site
title: Linn｜AI 测试与测试工程化笔记                        # 网站标题，显示在浏览器标签页和网站头部
# 副标题（可选）
# subtitle:
description: AI 系统测试与测试工程化｜大模型质量评估、Prompt 回归、Agent 链路测试｜自动化与平台实践  # 网站描述，用于SEO和社交媒体分享
keywords: AI测试,大模型测试,LLM评估,Prompt回归测试,Agent测试,RAG测试,测试工程化,自动化测试,接口测试,测试平台,质量保障,性能监控  # 网站关键词，用于SEO
author: yuxiaoling                 # 网站作者名称
language: zh-CN                    # 网站语言，影响日期格式等
timezone:  Asia/Shanghai    # 时区设置，影响文章发布时间显示

# ============================================
# URL 配置
# ============================================
# URL
## 设置你的网站URL，如果使用GitHub Pages，设置为 'https://username.github.io/project'
url: https://Linn0813.github.io  # 网站根URL，必须与GitHub Pages域名一致
permalink: :title/  # 文章永久链接格式，:year/:month/:day/:title/ 表示 年/月/日/标题/
permalink_defaults:  # 永久链接的默认值
pretty_urls:
  trailing_index: true # 设置为 false 时，永久链接末尾不包含 'index.html'
  trailing_html: true # 设置为 false 时，永久链接末尾不包含 '.html'

# ============================================
# 目录结构配置
# ============================================
# Directory
source_dir: source  # 源文件目录，存放Markdown文章
public_dir: public  # 输出目录，Hexo生成的静态文件存放位置
tag_dir: tags  # 标签页目录名
archive_dir: archives  # 归档页目录名
category_dir: categories  # 分类页目录名
code_dir: downloads/code  # 代码下载目录
i18n_dir: :lang  # 国际化文件目录
skip_render:  # 跳过渲染的文件或目录，支持正则表达式

# ============================================
# 写作配置
# ============================================
# Writing
new_post_name: :title.md # 新文章的文件名格式，:title 会被替换为文章标题
default_layout: post  # 默认布局，post 表示文章布局
titlecase: false # 是否将标题转换为标题格式（首字母大写）
external_link:
  enable: true # 是否在新标签页打开外部链接
  field: site # 应用范围：site（全站）或 post（仅文章）
  exclude: ''  # 排除的域名，这些域名的链接不会在新标签页打开
filename_case: 0  # 文件名大小写：0-不转换，1-小写，2-大写
render_drafts: false  # 是否渲染草稿文件
post_asset_folder: false  # 是否为每篇文章创建资源文件夹
relative_link: false  # 是否使用相对链接
future: true  # 是否显示未来日期的文章
syntax_highlighter: highlight.js  # 代码高亮引擎：highlight.js 或 prismjs
highlight:  # highlight.js 配置
  line_number: true  # 是否显示行号
  auto_detect: false  # 是否自动检测代码语言
  tab_replace: ''  # Tab 键替换字符
  wrap: true  # 是否换行
  hljs: false  # 是否使用 hljs 类名
prismjs:  # Prism.js 配置
  preprocess: true  # 是否预处理
  line_number: true  # 是否显示行号
  tab_replace: ''  # Tab 键替换字符

# ============================================
# 首页配置
# ============================================
# Home page setting
# path: 博客首页的根路径（默认为空）
# per_page: 每页显示的文章数量（0 表示禁用分页）
# order_by: 文章排序方式（默认按日期降序）
index_generator:
  path: ''  # 首页路径，空表示根路径
  per_page: 15  # 首页每页显示15篇文章
  order_by: -date  # 按日期降序排列（-date 表示降序，date 表示升序）

# ============================================
# 分类和标签配置
# ============================================
# Category & Tag
default_category: uncategorized  # 默认分类名称，未分类的文章会使用此分类
category_map:  # 分类映射，用于重命名分类
tag_map:  # 标签映射，用于重命名标签

# ============================================
# 元数据配置
# ============================================
# Metadata elements
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true  # 是否在HTML中生成meta generator标签

# ============================================
# 日期时间格式配置
# ============================================
# Date / Time format
## Hexo 使用 Moment.js 解析和显示日期
## 可以自定义日期格式，参考：http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD  # 日期格式：年-月-日
time_format: HH:mm:ss  # 时间格式：时:分:秒
## updated_option 支持的值：'mtime'（文件修改时间）、'date'（front matter中的日期）、'empty'（不显示）
updated_option: 'mtime'  # 文章更新时间来源

# ============================================
# 分页配置
# ============================================
# Pagination
## 设置 per_page 为 0 可禁用分页
per_page: 10  # 每页显示的文章数量
pagination_dir: page  # 分页目录名，如 /page/2/

# ============================================
# 包含/排除文件配置
# ============================================
# Include / Exclude file(s)
## include:/exclude: 选项仅适用于 'source/' 文件夹
include:  # 包含的文件或目录
exclude:  # 排除的文件或目录
ignore:  # 忽略的文件或目录

# ============================================
# 扩展配置
# ============================================
# Extensions
## 插件: https://hexo.io/plugins/
## 主题: https://hexo.io/themes/
theme: butterfly  # 使用的主题名称

# ============================================
# 部署配置
# ============================================
# Deployment
## 文档: https://hexo.io/docs/one-command-deployment
deploy:
  type: git  # 部署类型：git
  repo: https://github.com/Linn0813/Linn0813.github.io.git  # Git仓库地址
  branch: main  # 部署分支名称


# ============================================
# Butterfly 主题配置开始
# ============================================

# ============================================
# 导航栏设置
# ============================================
nav:
  # 导航栏 Logo 图片路径
  logo: /img/logo.jpg
  # 是否在导航栏显示网站标题
  display_title: true
  # 是否固定导航栏（滚动时保持可见）
  fixed: true

# ============================================
# 菜单配置
# ============================================
# 格式：菜单名: 链接 || 图标类名
menu:
  主页: / || fas fa-home  # 主页链接，使用 Font Awesome 图标
  分类: /categories/ || fa fa-archive  # 分类页面
  标签: /tags/ || fa fa-tags  # 标签页面
  归档: /archives/ || fa fa-folder-open  # 归档页面
  # 生活 || fas fa-list:  # 示例：带子菜单的菜单项
    # 分享: /shuoshuo/ || fa fa-comments-o
    # 相册: /photos/ || fa fa-camera-retro
    # 音乐: /music/ || fa fa-music
    # 影视: /movies/ || fas fa-video
  知识库问答: /qa/ || fa fa-comments  # 知识库问答页面
  友链: /links/ || fa fa-link  # 友链页面
  # 留言板: /comment/ || fa fa-paper-plane  # 已注释的留言板
  关于笔者: /about/ || fas fa-heart  # 关于页面


# ============================================
# 代码块设置
# ============================================
highlight_theme: mac  # 代码高亮主题：darker / pale night / light / ocean / mac / mac light / false
highlight_copy: true # 是否显示代码复制按钮
highlight_lang: false # 是否显示代码语言标签
highlight_shrink: true # true: 代码块默认折叠 / false: 代码块默认展开 | none: 展开并隐藏折叠按钮
highlight_height_limit: false # 代码块高度限制（单位：px），false 表示不限制
code_word_wrap: true # 代码自动换行，关闭横向滚动条

# ============================================
# 本地搜索功能配置
# ============================================
# Local search
local_search:
  enable: true  # 是否启用本地搜索
  labels:
    input_placeholder: 搜索文章、分类、标签...  # 搜索框占位符文本
    hits_empty: "未找到与「${query}」相关的内容" # 无搜索结果时显示的文本

  # 代码块工具栏配置
  # 是否显示复制按钮
  copy: true
  # 是否显示语言标签
  language: true
  # true: 收缩代码块 | false: 展开代码块 | none: 展开代码块并隐藏按钮
  shrink: false
  # 是否显示全屏显示代码块按钮
  fullpage: false

# ============================================
# 社交媒体链接配置
# ============================================
# 格式: 图标类名: 链接 || 描述 || 颜色（可选）
social:
  fab fa-github:  https://github.com/Linn0813 || GitHub  # GitHub链接
  fa fa-envelope-open-text: mailto:yuxiaoling.yxl@qq.com || Email  # 邮箱链接
  fab fa-weixin: javascript:alert('微信公众号：Linn的技术笔记') || 微信  # 微信（使用弹窗）


# ============================================
# 图片设置
# ============================================

# 网站的 favicon 图标（浏览器标签页小图标）
favicon: /img/favicon.jpg

# ============================================
# SEO 优化配置
# ============================================

# Sitemap 配置（搜索引擎站点地图）
sitemap:
  path: sitemap.xml  # sitemap文件路径

# RSS 订阅配置
feed:
  type: atom  # RSS类型：atom 或 rss2
  path: atom.xml  # RSS文件路径
  limit: 20  # RSS中显示的文章数量限制

# ============================================
# 头像设置
# ============================================
avatar: 
  # 头像图片链接
  img: https://i.loli.net/2021/02/24/5O1day2nriDzjSu.png
  # 是否启用头像旋转效果
  effect: false

# 是否禁用所有页面的顶部横幅图片
disable_top_img: false

# 如果页面未设置横幅图片，则显示默认的横幅图片
default_top_img: /img/default_top_img.jpg 

# 主页的横幅图片
index_img: /img/background.jpg  # 主页背景图片路径
# index_img: false  # 设置为 false 表示不使用背景图片

# 归档页的横幅图片
archive_img: /img/archive.jpg

# 注意: 这是单个标签页的横幅图片，不是标签列表页
tag_img: /img/tag_img.jpg

# 为每个标签设置单独的横幅图片
# 格式:
#  - 标签名: 图片链接
tag_per_img:

# 注意: 这是单个分类页的横幅图片，不是分类列表页
category_img: /img/category_img.jpg

# 为每个分类设置单独的横幅图片
# 格式:
#  - 分类名: 图片链接
category_per_img:

# 页脚的背景图片
footer_img: false  # false 表示不使用页脚背景图片

# 网站整体背景
# 可以设置为颜色（如：#ffffff）或图片（格式: url(http://xxxxxx.com/xxx.jpg)）
background:

# ============================================
# 文章封面设置
# ============================================
cover:
  # 是否在首页显示文章封面
  index_enable: true
  # 是否在侧边栏显示文章封面
  aside_enable: true
  # 是否在归档页显示文章封面
  archives_enable: true
  # 主页封面的位置：left（左侧）/ right（右侧）/ both（两侧）
  position: both
  # 当文章未设置封面时，显示的默认封面图片列表
  default_cover:
    # - https://i.loli.net/2020/05/01/gkihqEjXxJ5UZ1C.jpg

# ============================================
# 错误图片替换配置
# ============================================
# 替换损坏的图片
error_img:
  # 友链页面的错误图片（当友链头像加载失败时显示）
  flink: /img/friend_404.gif
  # 文章页面的错误图片（当文章图片加载失败时显示）
  post_page: /img/404.jpg

# ============================================
# 404 页面配置
# ============================================
# 简单的 404 页面
error_404:
  # 是否启用自定义404页面
  enable: true
  # 404页面的副标题文本
  subtitle: 'Page Not Found'
  # 404页面的卡片背景图片
  background: https://i.loli.net/2020/05/19/aKOcLiyPl2JQdFD.png


# ============================================
# 图片灯箱效果配置
# ============================================
# 图片大图查看
medium_zoom: false  # 是否使用 medium-zoom 灯箱效果
fancybox: true  # 是否使用 fancybox 灯箱效果（推荐）


# ============================================
# 文章元数据设置
# ============================================
post_meta:
  # 主页页面（文章列表页）的元数据设置
  page:
    # 日期类型：created（创建时间）/ updated（更新时间）/ both（两者都显示）
    date_type: created
    # 日期格式：date（日期格式）/ relative（相对时间，如"3天前"）
    date_format: date
    # 是否显示分类
    categories: true
    # 是否显示标签
    tags: false
    # 是否显示文字标签（如"发表于"、"分类"等）
    label: true
  # 文章详情页的元数据设置
  post:
    # 元数据位置：left（左侧）/ center（居中）
    position: left
    # 日期类型：created / updated / both
    date_type: both
    # 日期格式：date / relative
    date_format: date
    # 是否显示分类
    categories: true
    # 是否显示标签
    tags: true
    # 是否显示文字标签
    label: true

# ============================================
# 首页设置
# ============================================

# 首页头图的设置
# 默认: 头图全屏，站点信息在中间
# 站点信息的位置，例如: 300px/300em/300rem/10%（从顶部算起）
index_site_info_top:
# 头图的高度，例如: 300px/300em/300rem
index_top_img_height:

# ============================================
# 首页副标题设置
# ============================================
subtitle:
  enable: true  # 是否启用副标题
  # Typewriter Effect (打字机效果)
  effect: true  # 是否启用打字机效果
  # loop (循环打字，打字完成后重新开始)
  loop: true
  # source 调用第三方服务获取副标题
  # source: false  关闭调用
  # source: 1  调用搏天api的随机语录（简体）
  # source: 2  调用一言网的一句话（简体）
  # source: 3  调用一句网（简体）
  # source: 4  调用今日诗词（简体）
  # subtitle 会先显示 source 的内容，再显示 sub 的内容
  source: false
  # 如果有英文逗号' , ',请使用转义字符 &#44;
  # 如果有英文双引号' " ',请使用转义字符 &quot;
  # 开头不允许转义字符，如需要，请把整个句子用双引号包住
  # 如果关闭打字效果，subtitle只会显示sub的第一行文字
  sub:
    - 🎯 测试工程师｜聚焦 AI 系统测试与测试工程化
    - 🤖 大模型质量评估：输出一致性、幻觉/越界、Prompt 回归测试
    - 🧩 Agent / 工具调用链路测试：可观测性、异常注入、稳定性验证
    - 🔧 自动化测试体系建设：接口自动化、用例生成、持续回归
    - 🏗️ 测试平台实践：权限/日志/任务编排与效率工具链
    - 📚 以实战为主的技术沉淀：问题复盘、方法论总结、可复用模板

# ============================================
# 首页文章布局配置
# ============================================
# 1: 行布局（图片在左边）
# 2: 列布局（图片在右边）
# 3: 交替布局（图片左右交替显示）
index_layout: 3

# ============================================
# 首页文章简介显示配置
# ============================================
# 在首页显示文章简介
# 1: 描述（使用文章front matter中的description）
# 2: 两者（如果存在描述，将显示描述，否则显示自动摘要）
# 3: 自动摘要（默认，自动截取文章前N个字符）
# false: 不显示文章简介
index_post_content:
  method: 1  # 显示方式
  # 如果设置 method 为 2 或 3，需要配置长度（字符数）
  length: 500

# ============================================
# 文章设置
# ============================================

# ============================================
# 目录（TOC）设置
# ============================================
toc:
  # 是否在文章中显示目录
  post: true
  # 是否在页面中显示目录
  page: false
  # 是否显示目录编号（1, 1.1, 1.1.1等）
  number: true
  # 是否默认展开目录（显示所有标题）
  expand: false
  # 是否使用简洁风格（仅适用于文章）
  style_simple: false
  # 是否显示滚动百分比（阅读进度）
  scroll_percent: true

# ============================================
# 文章版权声明配置
# ============================================
post_copyright:
  # 是否启用版权声明
  enable: true
  # 是否进行文章 URL 解码
  decode: false
  # 作者链接（点击作者名跳转的链接）
  author_href:
  # 许可证类型
  license: CC BY-NC-SA 4.0
  # 许可证链接
  license_url: https://creativecommons.org/licenses/by-nc-sa/4.0/

# ============================================
# 打赏/赞助配置
# ============================================
reward:
  # 是否启用打赏功能
  enable: true
  # 打赏提示文本
  text:
  QR_code:  # 二维码配置
    # - img: /img/wechat.jpg  # 微信收款码图片
    #   link:  # 链接（可选）
    #   text: 微信  # 显示文本
    # - img: /img/alipay.jpg  # 支付宝收款码图片
    #   link:
    #   text: 支付宝

# ============================================
# 文章在线编辑配置
# ============================================
# 轻松在线浏览和编辑博客源代码
post_edit:
  # 是否启用在线编辑功能
  enable: false
  # GitHub编辑链接格式: https://github.com/用户名/仓库名/edit/分支名/子目录名/
  # 例如: https://github.com/jerryc127/butterfly.js.org/edit/main/source/
  url:

# ============================================
# 相关文章配置
# ============================================
related_post:
  # 是否显示相关文章
  enable: true
  # 显示的文章数量
  limit: 8
  # 选择: created（创建时间）/ updated（更新时间）
  date_type: created

# ============================================
# 文章分页配置
# ============================================
# 选择: 1 / 2 / false
# 1: "下一篇文章"将链接到旧文章（时间更早的文章）
# 2: "下一篇文章"将链接到新文章（时间更新的文章）
# false: 禁用分页
post_pagination: 1

# ============================================
# 文章过期通知配置
# ============================================
# 显示文章过期通知
noticeOutdate:
  # 是否启用过期通知
  enable: true
  # 样式: simple（简单样式）/ flat（扁平样式）
  style: flat
  # 多少天后显示通知（文章最后更新时间距离现在的天数）
  limit_day: 365
  # 位置: top（顶部）/ bottom（底部）
  position: top
  message_prev: 已经过了  # 通知前缀文本
  message_next: 天自上次更新，文章内容可能已过时。  # 通知后缀文本

# ============================================
# 页脚设置
# ============================================
footer:
  owner:
    # 是否启用所有者信息显示
    enable: true
    # 网站创建年份
    since: 2025
  # 自定义文本（显示在页脚）
  custom_text:
  # 是否显示主题和框架的版权声明
  copyright: true

# ============================================
# 侧边栏设置
# ============================================

aside:
  # 是否启用侧边栏
  enable: true
  # 是否默认隐藏侧边栏
  hide: false
  # 是否在右下角显示隐藏/显示侧边栏的按钮
  button: true
  # 移动设备上是否启用侧边栏
  mobile: true
  # 侧边栏位置：left（左侧）/ right（右侧）
  position: right
  display:
    # 归档页面是否显示侧边栏
    archive: true
    # 标签页面是否显示侧边栏
    tag: true
    # 分类页面是否显示侧边栏
    category: true
  
  # ============================================
  # 作者信息卡片配置
  # ============================================
  card_author:
    # 是否显示作者信息卡片
    enable: true
    # 作者描述（显示在头像下方）
    description:
    button:
      # 是否显示按钮
      enable: true
      # 按钮图标（Font Awesome 类名）
      icon: fab fa-github
      # 按钮文本
      text: Follow Me
      # 按钮链接
      link: https://github.com/Linn0813
  
  # ============================================
  # 公告卡片配置
  # ============================================
  card_announcement:
    # 是否显示公告卡片
    enable: true
    # 公告内容
    content: 最近在做：AI 生成测试用例与质量评估
  
  # ============================================
  # 最近文章卡片配置
  # ============================================
  card_recent_post:
    # 是否显示最近文章卡片
    enable: true
    # 显示文章数量，0 表示显示所有
    limit: 5
    # 排序方式：date（创建时间）/ updated（更新时间）
    sort: date
    sort_order:  # 排序顺序（可选）
  
  # ============================================
  # 最新评论卡片配置
  # ============================================
  card_newest_comments:
    # 是否显示最新评论卡片
    enable: true
    sort_order:  # 排序顺序（可选）
    # 显示评论数量
    limit: 6
    # 单位：分钟，保存数据到 localStorage（减少缓存时间以加快评论显示）
    storage: 1
    # 是否显示头像
    avatar: true
  
  # ============================================
  # 分类卡片配置
  # ============================================
  card_categories:
    # 是否显示分类卡片
    enable: true
    # 显示分类数量，0 表示显示所有
    limit: 8
    # 选择：none（不展开）/ true（展开）/ false（折叠）
    expand: none
    sort_order:  # 排序顺序（可选）
  
  # ============================================
  # 标签卡片配置
  # ============================================
  card_tags:
    # 是否显示标签卡片
    enable: true
    # 显示标签数量，0 表示显示所有
    limit: 40
    # 是否启用标签颜色（随机颜色）
    color: false
    # 标签排序方式：random（随机）/ name（按名称）/ length（按文章数量）
    orderby: length
    # 排序顺序：1 表示升序，-1 表示降序
    order: -1
    sort_order:  # 排序顺序（可选）
  
  # ============================================
  # 归档卡片配置
  # ============================================
  card_archives:
    # 是否显示归档卡片
    enable: true
    # 归档类型：monthly（按月）/ yearly（按年）
    type: monthly
    # 日期格式，例如：MMMM YYYY（显示为"2025年1月"）
    format: MMMM YYYY
    # 排序顺序：1 表示升序，-1 表示降序
    order: -1
    # 显示归档数量，0 表示显示所有
    limit: 8
    sort_order:  # 排序顺序（可选）
  
  # ============================================
  # 系列文章卡片配置
  # ============================================
  card_post_series:
    # 是否显示系列文章卡片
    enable: false
    # 标题是否显示系列名称
    series_title: false
    # 排序方式：title（按标题）或 date（按日期）
    orderBy: 'date'
    # 排序顺序：1 表示升序，-1 表示降序
    order: -1
  
  # ============================================
  # 网站信息卡片配置
  # ============================================
  card_webinfo:
    # 是否显示网站信息卡片
    enable: true
    # 是否显示文章总数
    post_count: true
    # 是否显示最后推送日期
    last_push_date: true
    sort_order:  # 排序顺序（可选）
    # 网站运行时间（从创建日期到当前日期的时间差）
    # 格式：Month/Day/Year Time 或 Year/Month/Day Time
    # 如果不启用此功能，请留空
    runtime_date:

# ============================================
# 右下角按钮设置
# ============================================

# 右下角按钮与底部的距离（默认单位：px）
rightside_bottom:

# ============================================
# 简繁转换设置
# ============================================
translate:
  # 是否启用简繁转换功能
  enable: false
  # 按钮默认文本
  default: 繁
  # 网站默认语言（1 - 繁体中文 / 2 - 简体中文）
  defaultEncoding: 2
  # 转换延迟（毫秒）
  translateDelay: 0
  # 按钮在简体中文时的文本
  msgToTraditionalChinese: '繁'
  # 按钮在繁体中文时的文本
  msgToSimplifiedChinese: '简'

# ============================================
# 阅读模式设置
# ============================================
readmode:
  enable: true  # 是否启用阅读模式
  # 阅读模式下自动隐藏侧边栏
  auto_hide_aside: true

# ============================================
# 暗黑模式设置
# ============================================
darkmode:
  # 是否启用暗黑模式
  enable: true
  # 是否显示切换暗黑/明亮模式的按钮
  button: true
  # 是否自动切换暗黑/明亮模式
  # autoChangeMode: 1  跟随系统设置，如果系统不支持暗黑模式，则在晚上 6 点到早上 6 点之间切换暗黑模式
  # autoChangeMode: 2  在晚上 6 点到早上 6 点之间切换暗黑模式
  # autoChangeMode: false  不自动切换
  autoChangeMode: false
  # 设置明亮模式时间，值在 0 到 24 之间。如果未设置，默认值为 6 和 18
  start:  # 开始时间（小时）
  end:  # 结束时间（小时）

# 在返回顶部按钮中显示滚动百分比
rightside_scroll_percent: true

# ============================================
# 右下角按钮顺序设置
# ============================================
# 不要修改以下设置，除非你知道它们的工作原理
# 选择：readmode（阅读模式）,translate（简繁转换）,darkmode（暗黑模式）,hideAside（隐藏侧边栏）,toc（目录）,chat（聊天）,comment（评论）
# 不要重复相同的值
rightside_item_order:
  # 是否启用右侧项目顺序自定义
  enable: false
  # 隐藏的默认项目：readmode,translate,darkmode,hideAside
  hide:
  # 显示的默认项目：toc,chat,comment
  show:

# ============================================
# 全局设置
# ============================================

# ============================================
# 锚点设置
# ============================================
anchor:
  # 滚动时，URL 将根据标题 ID 自动更新（如 #标题名）
  auto_update: true
  # 点击标题时滚动到对应位置并更新锚点
  click_to_scroll: true

# 是否显示图片标题（figcaption）
photofigcaption: true

# ============================================
# 复制设置
# ============================================
copy:
  enable: true # 是否开启网站复制权限
  copyright:  # 复制的内容后面加上版权信息
    enable: false  # 是否开启复制版权信息添加
    limit_count: 50 # 字数限制，当复制文字大于这个字数限制时才添加版权信息


# ============================================
# 字数统计配置
# ============================================
# 需要安装 hexo-wordcount 插件
wordcount:
  # 是否启用字数统计
  enable: true
  # 在文章元信息中显示字数统计
  post_wordcount: true
  # 在文章元信息中显示阅读时间（预计阅读分钟数）
  min2read: true
  # 在侧边栏网站信息中显示总字数
  total_wordcount: true

# ============================================
# 访问统计配置（不蒜子）
# ============================================
# 不蒜子 PV / UV 统计
busuanzi:
  # 网站 UV 统计（独立访客数）
  site_uv: true
  # 网站 PV 统计（页面浏览量）
  site_pv: true
  # 页面 PV 统计（单个页面的浏览量）
  page_pv: true

# ============================================
# 数学公式设置
# ============================================
# 关于 per_page
# 如果设置为 true，将在每个页面加载 mathjax/katex 脚本
# 如果设置为 false，将根据你的设置加载 mathjax/katex 脚本（在页面的 front-matter 中添加 'mathjax: true' 或者 'katex: true'）
math:
  # 选择：mathjax（MathJax引擎）, katex（KaTeX引擎，更快）
  # 如果不需要数学公式，保持为空
  use: katex
  per_page: false  # 是否每页都加载数学公式脚本
  hide_scrollbar: false  # 是否隐藏滚动条

  mathjax:  # MathJax 配置
    # 启用上下文菜单（右键菜单）
    enableMenu: true
    # 选择：all（所有公式编号）/ ams（仅AMS公式编号）/ none（不编号）
    tags: none

  katex:  # KaTeX 配置
    # 启用复制 KaTeX 公式功能
    copy_tex: true

# ============================================
# 搜索设置
# ============================================
search:
  # 选择：algolia_search（Algolia搜索）/ local_search（本地搜索）/ docsearch（Docsearch）
  # 如果不需要搜索功能，保持为空
  use: local_search
  placeholder: 搜索文章、分类、标签...  # 搜索框占位符

  # Algolia 搜索配置
  algolia_search:
    # 每页搜索结果数量
    hitsPerPage: 6

  # 本地搜索配置
  local_search:
    # 页面加载时预加载搜索数据（加快搜索速度）
    preload: true
    # 每篇文章显示的顶部 n 个搜索结果，设置为 -1 显示所有结果
    top_n_per_article: 3
    # 将 HTML 字符串反转义为可读内容
    unescape: true
    CDN:  # CDN地址（可选）

  # Docsearch 配置
  # https://docsearch.algolia.com/
  docsearch:
    appId:  # 应用ID
    apiKey:  # API密钥
    indexName:  # 索引名称
    option:  # 其他选项

# ============================================
# 分享系统配置
# ============================================
share:
  # 选择：sharejs（Share.js）/ addtoany（AddToAny）
  # 如果不需要分享功能，保持为空
  use: sharejs

  # Share.js 配置
  # https://github.com/overtrue/share.js
  sharejs:
    sites: wechat,qq  # 启用的分享平台

  # AddToAny 配置
  # https://www.addtoany.com/
  addtoany:
    item: wechat,qq,email,copy_link  # 分享项

# ============================================
# 评论系统配置
# ============================================
comments:
  # 最多两个评论系统，第一个将作为默认显示
  # 如果不需要评论功能，保持为空
  # 选择：Disqus/Disqusjs/Livere/Gitalk/Valine/Waline/Utterances/Facebook Comments/Twikoo/Giscus/Remark42/Artalk
  # 两个评论系统的格式：Disqus,Waline
  use: Utterances
  # 按钮旁边是否显示评论系统名称
  text: true
  # 懒加载：评论系统将在评论元素进入浏览器视口时加载（提升页面加载速度）
  # 如果设置为 true，评论计数将无效
  lazyload: false
  # 在文章顶部图片中显示评论计数
  count: false
  # 在主页显示评论计数
  card_post_count: false

# Disqus 评论插件配置
# 官方文档：https://disqus.com/
disqus:
  # Disqus 的 shortname（短名称）
  shortname:
  # 最新评论小部件的 API 密钥
  apikey:

# 使用 Disqus API 渲染评论的替代方案（国内可用）
# 官方文档：https://github.com/SukkaW/DisqusJS
disqusjs:
  # Disqus 的 shortname
  shortname:
  # API 密钥
  apikey:
  # 其他可选配置
  option:

# Livere 评论插件配置（来必力）
# 官方文档：https://www.livere.com/
livere:
  # Livere 的用户 ID
  uid:

# Gitalk 评论插件配置（基于GitHub Issues）
# 官方文档：https://github.com/gitalk/gitalk
gitalk:
  # GitHub 应用的客户端 ID
  client_id:
  # GitHub 应用的客户端密钥
  client_secret:
  # 存储评论的仓库名称
  repo:
  # 仓库拥有者的用户名
  owner:
  # 管理员用户名列表
  admin:
  # 其他可选配置
  option:

# Valine 评论插件配置（基于LeanCloud）
# 官方文档：https://valine.js.org
valine:
  # LeanCloud 应用的 appId
  appId:
  # LeanCloud 应用的 appKey
  appKey:
  # 评论者头像样式（如：monsterid, wavatar等）
  avatar: monsterid
  # 该配置适用于国内自定义域名用户，海外版本将自动检测（无需手动填写）
  serverURLs:
  # 评论框背景图片
  bg:
  # 使用 Valine 的访客计数作为页面的访客量
  visitor: false
  # 其他可选配置
  option:

# Waline 评论插件配置（Valine的升级版，支持后端）
# 官方文档：https://waline.js.org/
waline:
  # 服务器 URL（需要自己部署）
  serverURL:
  # 评论框背景图片
  bg:
  # 使用 Waline 的访客计数作为页面的访客量
  pageview: false
  # 其他可选配置
  option:

# Utterances 评论插件配置（基于GitHub Issues，无需后端）
# 官方文档：https://utteranc.es/
utterances:
  # 存储评论的 GitHub 仓库（格式：用户名/仓库名）
  repo: Linn0813/Linn0813.github.io
  # 问题映射方式，可选值：pathname（路径）/url（完整URL）/title（标题）/og:title（Open Graph标题）
  issue_term: pathname
  # 浅色主题，可选值：github-light
  light_theme: github-light
  # 深色主题，可选值：photon-dark
  dark_theme: photon-dark

# Facebook 评论插件配置
# 官方文档：https://developers.facebook.com/docs/plugins/comments/
facebook_comments:
  # 应用 ID
  app_id:
  # 用户 ID，可选
  user_id:
  # 每页显示评论数
  pageSize: 10
  # 评论排序方式，可选值：social（社交排序）/ time（时间排序）/ reverse_time（反向时间排序）
  order_by: social
  # 语言设置
  lang: zh_TW

# Twikoo 评论插件配置（基于腾讯云开发）
# 官方文档：https://github.com/imaegoo/twikoo
twikoo:
  # 环境 ID（腾讯云开发环境ID）
  envId:
  # 区域（如：ap-shanghai）
  region:
  # 使用 Twikoo 的访客计数作为页面的访客量
  visitor: false
  # 其他可选配置
  option:

# Giscus 评论插件配置（基于GitHub Discussions）
# 官方文档：https://giscus.app/
giscus:
  # 仓库地址（格式：用户名/仓库名）
  repo:
  # 仓库 ID（可在GitHub API获取）
  repo_id:
  # 分类 ID（GitHub Discussions分类ID）
  category_id:
  # 主题配置，light 为浅色主题，dark 为深色主题
  theme:
    light: light
    dark: dark
  # 其他可选配置
  option:

# Remark42 评论插件配置
# 官方文档：https://remark42.com/docs/configuration/frontend/
remark42:
  # 服务器地址
  host:
  # 站点 ID
  siteId:
  # 其他可选配置
  option:

# Artalk 评论插件配置
# 官方文档：https://artalk.js.org/guide/frontend/config.html
artalk:
  # 服务器地址
  server:
  # 站点名
  site:
  # 使用 Artalk 的访客计数作为页面的访客量
  visitor: false
  # 其他可选配置
  option:

# ============================================
# 聊天服务配置
# ============================================
chat:
  # 聊天服务类型，可选值：chatra（Chatra）/tidio（Tidio）/crisp（Crisp），如果不需要聊天功能则留空
  use:
  # 推荐使用聊天按钮，会在网站右下角创建一个按钮，并隐藏原始按钮
  rightside_button: false
  # 原始聊天按钮在向上滚动时显示，向下滚动时隐藏
  button_hide_show: false

# Chatra 聊天服务配置
# 官方网站：https://chatra.io/
chatra:
  # Chatra 服务 ID
  id:

# Tidio 聊天服务配置
# 官方网站：https://www.tidio.com/
tidio:
  # Tidio 公钥
  public_key:

# Crisp 聊天服务配置
# 官方网站：https://crisp.chat/en/
crisp:
  # Crisp 网站 ID
  website_id:

# ============================================
# 分析服务配置
# ============================================

# 百度统计配置
# 官方网站：https://tongji.baidu.com/web/welcome/login
baidu_analytics:  # 百度统计ID

# 谷歌分析配置
# 官方网站：https://analytics.google.com/analytics/web/
google_analytics:  # Google Analytics ID（如：G-XXXXXXXXXX）

# Cloudflare 分析配置
# 官方网站：https://www.cloudflare.com/zh-tw/web-analytics/
cloudflare_analytics:  # Cloudflare Analytics Token

# Microsoft Clarity 分析配置
# 官方网站：https://clarity.microsoft.com/
microsoft_clarity:  # Microsoft Clarity ID

# ============================================
# 广告配置
# ============================================

# Google Adsense 广告配置
google_adsense:
  # 是否启用
  enable: false
  # 是否自动投放广告
  auto_ads: true
  # 广告脚本 URL
  js: https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js
  # 客户 ID（如：ca-pub-xxxxxxxxxx）
  client:
  # 是否启用页面级广告
  enable_page_level_ads: true

# 手动插入广告配置，如果不需要广告则留空
ad:
  # 在首页每三个帖子后插入广告（HTML代码）
  index:
  # 在侧边栏插入广告（HTML代码）
  aside:
  # 在文章分页前插入广告（HTML代码）
  post:

# ============================================
# 站点验证配置
# ============================================
# 用于验证网站所有权（搜索引擎、社交媒体平台等）
site_verification:
  - name: google-site-verification  # 验证类型
    content: Xj7zV_DKnHFmJ7sXNRvdl5-UTfG4NLy-s7hDk1DZt4A  # 验证码
  # - name: baidu-site-verification  # 百度站长验证
  #   content: xxxxxxx

# ============================================
# 美化 / 效果配置
# ============================================

# 主题颜色自定义
# 注意：颜色值必须用双引号，如 "#000"，否则可能会导致错误！

# 主题颜色配置（已注释，如需启用请取消注释）
# theme_color:
#   是否启用主题颜色
#   enable: true
#   主颜色（链接、按钮等主要元素的颜色）
#   main: "#49B1F5"
#   分页器颜色
#   paginator: "#00c4b6"
#   按钮悬停颜色
#   button_hover: "#FF7242"
#   文本选择颜色（选中文本的背景色）
#   text_selection: "#00c4b6"
#   链接颜色
#   link_color: "#99a9bf"
#   元数据颜色（作者、日期等信息的颜色）
#   meta_color: "#858585"
#   水平线颜色
#   hr_color: "#A4D8FA"
#   代码前景色（代码文字颜色）
#   code_foreground: "#F47466"
#   代码背景色
#   code_background: "rgba(27, 31, 35, .05)"
#   目录颜色
#   toc_color: "#00c4b6"
#   引用块填充颜色
#   blockquote_padding_color: "#49b1f5"
#   引用块背景颜色
#   blockquote_background_color: "#49b1f5"
#   滚动条颜色
#   scrollbar_color: "#49b1f5"
#   浅色模式下的主题颜色
#   meta_theme_color_light: "ffffff"
#   深色模式下的主题颜色
#   meta_theme_color_dark: "#0d0d0d"

# 分类和标签页面的用户界面设置
# 选择：index（与主页 UI 相同）/ default（与归档 UI 相同）
# 留空或设置为 index
category_ui: default
# 标签页UI设置（留空表示使用默认）
tag_ui:

# 文本两端对齐（拉伸行使每行宽度相等）
text_align_justify: false

# 为页眉和页脚添加遮罩（半透明效果）
mask:
  header: true  # 页眉遮罩
  footer: true  # 页脚遮罩

# ============================================
# 加载动画配置
# ============================================
preloader:
  # 是否启用加载动画
  enable: true
  # 资源类型
  # 1. 全屏加载（全屏加载动画）
  # 2. 进度条（顶部进度条）
  source: 2
  # pace 主题 CSS URL（自定义进度条样式）
  # 参见 https://codebyzach.github.io/pace/
  pace_css_url:

# 页面过渡效果（页面切换时的动画效果）
enter_transitions: true

# 默认显示模式 - light（明亮模式，默认）/ dark（暗黑模式）
display_mode: light

# ============================================
# 美化文章内容配置
# ============================================
beautify:
  # 是否启用美化
  enable: true
  # 指定美化的范围：site（全站）或 post（仅文章）
  field: post
  # 指定标题前缀图标（Font Awesome Unicode）
  title-prefix-icon: '\f0c1'
  # 指定标题前缀图标的颜色
  title-prefix-icon-color: '#F47466'

# ============================================
# 全局字体设置
# ============================================
# 除非您知道它们的工作原理，否则不要修改以下设置
font:
  global-font-size:  # 全局字体大小
  code-font-size:  # 代码字体大小
  font-family:  # 字体族（如：'Microsoft YaHei', Arial, sans-serif）
  code-font-family:  # 代码字体族

# 网站标题和副标题的字体设置
blog_title_font:
  font_link:  # 字体链接（Google Fonts等）
  font-family:  # 字体族

# ============================================
# 分隔符图标设置
# ============================================
hr_icon:
  # 是否启用分隔符图标（文章中的水平线图标）
  enable: true
  # Font Awesome 图标的 unicode 值，如 '\3423'
  icon:
  icon-top:  # 顶部图标

# ============================================
# 打字机效果配置
# ============================================
# https://github.com/disjukr/activate-power-mode
activate_power_mode:
  # 是否启用打字机效果（打字时的特效）- 已禁用以提高性能
  enable: false
  # 是否启用彩色效果
  colorful: true
  # 是否启用震动效果
  shake: true
  # 是否在移动设备上启用
  mobile: false

# ============================================
# 背景效果配置
# ============================================

# canvas_ribbon（彩带背景效果）
# 参见: https://github.com/hustcc/ribbon.js
canvas_ribbon:
  # 是否启用 canvas_ribbon
  enable: false
  # ribbon 的大小
  size: 150
  # ribbon 的不透明度 (0 ~ 1)
  alpha: 0.6
  zIndex: -1  # 层级（负值表示在背景层）
  # 是否点击更改颜色
  click_to_change: false
  # 是否在移动设备上启用
  mobile: false

# Fluttering Ribbon（飘动彩带效果）
canvas_fluttering_ribbon:
  # 是否启用 Fluttering Ribbon
  enable: false
  # 是否在移动设备上启用
  mobile: false

# canvas_nest（粒子连线背景效果）
# https://github.com/hustcc/canvas-nest.js
canvas_nest:
  # 是否启用 canvas_nest - 已禁用以提高性能
  enable: false
  # 线条颜色，默认: '0,0,0'; RGB 值: (R,G,B).(注意: 使用 ',' 分隔.)
  color: '100,149,237'
  # 线条的不透明度 (0~1)
  alpha: 0.7
  zIndex: -1
  # 线条数量
  count: 99
  # 是否在移动设备上启用
  mobile: false

# ============================================
# 鼠标点击效果配置
# ============================================

# 鼠标点击效果: 烟花
fireworks:
  # 是否启用烟花效果
  enable: false
  zIndex: 9999  # 层级
  # 是否在移动设备上启用
  mobile: false

# 鼠标点击效果: 心形符号
click_heart:
  # 是否启用心形符号效果 - 已禁用以提高性能
  enable: false
  # 是否在移动设备上启用
  mobile: false

# 鼠标点击效果: 文字
clickShowText:
  # 是否启用文字效果
  enable: false
  text:  # 显示的文字列表
    # - I
    # - LOVE
    # - YOU
  fontSize: 15px  # 字体大小
  # 是否随机显示文字
  random: false
  # 是否在移动设备上启用
  mobile: false

# ============================================
# 灯箱设置
# ============================================
# 选择: fancybox（Fancybox插件）/ medium_zoom（Medium Zoom插件）
# https://github.com/francoischalifour/medium-zoom
# https://fancyapps.com/fancybox/
# 如果不需要灯箱效果，请留空
lightbox: fancybox

# ============================================
# 标签外挂设置
# ============================================

# 系列文章配置
series:
  # 是否启用系列文章功能
  enable: false
  # 按标题或日期排序
  orderBy: 'title'
  # 排序方式。1, asc 为升序; -1, desc 为降序
  order: 1
  # 是否显示编号
  number: true

# ABCJS - ABC 音乐符号插件
# https://github.com/paulrosen/abcjs
abcjs:
  # 是否启用 ABCJS（用于显示乐谱）
  enable: false
  # 是否每页启用
  per_page: true

# Mermaid 图表插件
# https://github.com/mermaid-js/mermaid
mermaid:
  # 是否启用 Mermaid（用于绘制流程图、时序图等）
  enable: false
  # 使用代码块编写 Mermaid 图表
  code_write: false
  # 内置主题: default / forest / dark / neutral
  theme:
    light: default  # 浅色模式主题
    dark: dark  # 深色模式主题

# Chart.js 图表插件
# 参见 https://www.chartjs.org/docs/latest/
chartjs:
  enable: false  # 是否启用 Chart.js（用于绘制各种图表）
  # 除非你了解它们的工作原理，否则不要修改。
  # 默认设置仅在未指定 MD 语法时使用。
  # 图表的字体颜色
  fontColor:
    light: "rgba(0, 0, 0, 0.8)"  # 浅色模式字体颜色
    dark: "rgba(255, 255, 255, 0.8)"  # 深色模式字体颜色
  # 图表的边框颜色
  borderColor:
    light: "rgba(0, 0, 0, 0.1)"  # 浅色模式边框颜色
    dark: "rgba(255, 255, 255, 0.2)"  # 深色模式边框颜色
  # 雷达图和极区图的刻度标签背景颜色
  scale_ticks_backdropColor:
    light: "transparent"  # 浅色模式背景色
    dark: "transparent"  # 深色模式背景色

# Note - Bootstrap 提示框配置
note:
  # Note 标签样式值:
  #  - simple    bs-callout 旧警告样式。默认。
  #  - modern    bs-callout 新 (v2-v3) 警告样式。
  #  - flat      扁平提示框样式，带背景，如 Mozilla 或 StackOverflow。
  #  - disabled  禁用所有 Note 标签的 CSS 样式。
  style: flat
  # 是否显示图标
  icons: true
  # 边框半径（圆角大小）
  border_radius: 3
  # 背景颜色偏移百分比 (modern: -12 | 12; flat: -18 | 6)。
  # 也应用于标签变量。此选项可与禁用的 Note 标签一起使用。
  light_bg_offset: 0

# ============================================
# 其他设置
# ============================================

# Pjax 配置（无刷新页面切换）
# https://github.com/MoOx/pjax
pjax:
  # 是否启用 pjax（提升页面切换速度）
  enable: false
  # 排除指定页面不使用 pjax，如 '/music/'
  exclude:
    # - /xxxxxx/

# 注入 CSS 和脚本 (aplayer/meting 音乐播放器)
aplayerInject:
  # 是否启用注入
  enable: false
  # 是否每页启用
  per_page: true

# ============================================
# Snackbar - Toast 通知配置
# ============================================
# https://github.com/polonel/SnackBar
# 位置: top-left / top-center / top-right / bottom-left / bottom-center / bottom-right
snackbar:
  # 是否启用 Snackbar（消息提示）
  enable: false
  # 通知位置
  position: bottom-left
  # 浅色模式和深色模式下的通知背景颜色
  bg_light: '#49b1f5'  # 浅色模式背景色
  bg_dark: '#1f1f1f'  # 深色模式背景色

# Instant.page（预加载链接页面）
# https://instant.page/
instantpage: false  # 是否启用 Instant.page（鼠标悬停时预加载链接）

# ============================================
# 图片懒加载配置
# ============================================
Lazyload:
  # 是否启用 Lazyload（图片懒加载，提升页面加载速度）
  enable: true
  loadingImg: /img/loading.gif  # 加载中的占位图片
  # 使用浏览器的原生 lazyload 而不是 vanilla-lazyload
  native: true
  # 指定使用 Lazyload 的范围：site（全站）或 post（仅文章）
  field: site
  placeholder:  # 占位符（可选）
  blur: true  # 是否启用模糊效果（加载时显示模糊图片）

# ============================================
# PWA 配置（渐进式Web应用）
# ============================================
# 参见 https://github.com/JLHwung/hexo-offline
pwa:
  # 是否启用 PWA（支持离线访问、添加到主屏幕等）
  enable: false
  # PWA manifest 文件路径
  manifest:
  # Apple Touch 图标路径（iOS设备添加到主屏幕时的图标）
  apple_touch_icon:
  # 32x32 像素的 favicon 图标路径
  favicon_32_32:
  # 16x16 像素的 favicon 图标路径
  favicon_16_16:
  # mask 图标路径（Safari pinned tab图标）
  mask_icon:

# ============================================
# Open Graph meta 标签配置
# ============================================
# 参见 https://hexo.io/docs/helpers#open-graph
# 用于社交媒体分享时的预览信息
Open_Graph_meta:
  # 是否启用 Open Graph meta 标签
  enable: true
  option:
    # twitter_card:  # Twitter卡片类型
    # twitter_image:  # Twitter分享图片
    # twitter_id:  # Twitter用户ID
    # twitter_site:  # Twitter网站账号
    # google_plus:  # Google+配置（已废弃）
    # fb_admins:  # Facebook管理员ID
    # fb_app_id:  # Facebook应用ID

# ============================================
# 结构化数据配置
# ============================================
# https://developers.google.com/search/docs/guides/intro-structured-data
# 用于搜索引擎更好地理解网站内容
structured_data: true

# ============================================
# CSS 前缀配置
# ============================================
# 添加供应商前缀以确保兼容性（如：-webkit-, -moz-等）
# 是否启用 CSS 前缀
css_prefix: true

# ============================================
# 代码注入配置
# ============================================
# Inject
# 插入代码到 head（在 '</head>' 标签之前）和底部（在 '</body>' 标签之前）
inject:
  head:  # 插入到 <head> 标签中的代码
    - <link rel="stylesheet" href="/css/blog-enhancements.css">  # 自定义CSS
    - <meta name="msvalidate.01" content="C0AF818C725A2A1E112A1537E1064EA8" />  # 必应网站验证
  bottom:  # 插入到 </body> 标签之前的代码
    - <script src="/js/blog-enhancements.js"></script>  # 自定义JS
    - <script src="/js/category-enhancements.js"></script>
    - <script src="/js/archive-enhancements.js"></script>
    - <script src="/js/tag-enhancements.js"></script>
    - <script src="/js/index-enhancements.js"></script>

# ============================================
# CDN 设置
# ============================================
# 除非你知道它们的工作原理，否则不要修改以下设置
CDN:
  # 内部和第三方脚本的 CDN 提供商
  # 两者的选项：local（本地）/jsdelivr（jsDelivr CDN）/unpkg（unpkg CDN）/cdnjs（cdnjs CDN）/custom（自定义）
  # 注意： Dev 版本只能使用 'local' 作为内部脚本
  # 注意：将第三方脚本设置为 'local' 时，需要安装 hexo-butterfly-extjs
  internal_provider: local  # 内部脚本CDN提供商
  third_party_provider: jsdelivr  # 第三方脚本CDN提供商

  # 是否在 URL 中添加版本号，true 或 false（用于缓存控制）
  version: false

  # 自定义CDN格式
  # 例如：https://cdn.staticfile.org/${cdnjs_name}/${version}/${min_cdnjs_file}
  custom_format:

  option:  # 其他选项
