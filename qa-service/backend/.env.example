# ============================================
# 问答系统后端环境变量配置
# ============================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# --------------------------------------------
# LLM 配置
# --------------------------------------------
# LLM 服务地址（本地部署的 Ollama 或 OpenAI 兼容 API）
AI_DEMO_LLM_BASE_URL=http://localhost:11434

# 默认使用的模型名称
AI_DEMO_DEFAULT_MODEL=qwen2.5:7b

# --------------------------------------------
# 后端服务配置
# --------------------------------------------
# 服务监听地址（0.0.0.0 表示监听所有网络接口）
AI_DEMO_BACKEND_HOST=0.0.0.0

# 服务监听端口
AI_DEMO_BACKEND_PORT=8113

# 开发模式自动重载（生产环境应设置为 false）
AI_DEMO_BACKEND_RELOAD=true

# --------------------------------------------
# CORS 配置（重要！）
# --------------------------------------------
# 允许跨域请求的来源列表，用逗号分隔
# 生产环境必须明确指定前端域名，不要使用 "*"
# 示例：https://linn0813.github.io,http://localhost:4000
AI_DEMO_CORS_ORIGINS=https://linn0813.github.io,http://localhost:4000

# --------------------------------------------
# 飞书应用配置
# --------------------------------------------
# 飞书应用 ID（从飞书开放平台获取）
FEISHU_APP_ID=your_app_id

# 飞书应用密钥（从飞书开放平台获取）
FEISHU_APP_SECRET=your_app_secret

# OAuth 回调地址（用户身份权限需要）
FEISHU_REDIRECT_URI=http://localhost:8113/api/v1/feishu/oauth/callback

# 用户身份权限配置（可选）
# 设置为 true 使用用户身份权限，false 使用应用身份权限
FEISHU_USE_USER_TOKEN=false

# --------------------------------------------
# 前端配置
# --------------------------------------------
# 前端地址（用于 OAuth 回调重定向）
FRONTEND_URL=http://localhost:4000
